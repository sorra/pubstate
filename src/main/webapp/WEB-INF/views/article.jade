extends layout.jade

block customHeader
  title= article.title

block mainContent
  #article
    h1= article.title
    div
      +userLabel(article.author)
      +humanTimeLabel(article.whenCreated)
    div=! article.outputContent
